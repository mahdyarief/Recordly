# Ensures all dependencies are hoisted to the root level for native module resolution
# This is crucial for Electron projects with many native dependencies
shamefully-hoist=true

# Prevent pnpm from failing if some dependencies are missing (legacy peer deps)
strict-peer-dependencies=false

# Store configuration
store-dir=.pnpm-store
