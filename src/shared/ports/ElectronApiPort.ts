/**
 * ElectronApiPort — abstract contract for all Electron IPC interactions.
 * Prevents direct `window.electronAPI` usage outside adapters.
 */
export interface ElectronApiPort {
	// Methods to be defined as they are extracted during refactoring
}
