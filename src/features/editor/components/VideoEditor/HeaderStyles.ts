/**
 * Shared styles for action buttons in the application header.
 * Extracted into a separate file to comply with React's Fast Refresh rules
 * (files with components should only export components).
 */
export const APP_HEADER_ACTION_BUTTON_CLASS =
	"h-7 px-2 text-xs text-slate-400 hover:bg-white/10 hover:text-slate-200 transition-all gap-1.5";
